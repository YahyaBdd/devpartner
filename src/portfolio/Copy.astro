---
import en from '../i18n/en.json';
const {k,tag:Tag='p',class:className=''}=Astro.props;
---
<Tag class={className} data-i18n={k}>{en[k as keyof typeof en]?.replace('{year}', String(new Date().getFullYear()))}</Tag>
